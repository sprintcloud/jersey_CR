Esta web envia accion: compra y accion: ride, compatibles con el Apps Script actualizado en la conversacion. No reemplaces el Apps Script con archivos antiguos. Actualiza la implementacion de Apps Script a una Nueva version y verifica que la URL API_URL en index.html sea la URL /exec vigente. La web usa fetch no-cors: una solicitud enviada NO garantiza guardado; confirma el resultado mirando la pestaña Social Ride. El boton no solicita ni envia nombre o telefono y no bloquea intentos posteriores.
